


# Calendar-fu - Сервис интернет-календарей для университетского расписания

**Павел Сафонов, ТРПО25-5, 2026 год**

Веб-сервис для конвертации университетского расписания в формат iCalendar (.ical), совместимый с Google Calendar, Apple Calendar и другими календарными приложениями. Получает данные из API «Галактика: Расписание учебных занятий» Финансового Университета.

---

## 🚀 Быстрый старт

### Локальный запуск (для разработки)

1. **Установите зависимости:**
```bash
pip install -r requirements.txt
```

2. **Запустите сервис:**
```bash
python -m calendar_fu -D --file
```

3. **Откройте в браузере:**
```
http://localhost:4040/
```

**Опции запуска:**
- `-D` — режим отладки (debug mode)
- `--file` — использование локальных .ics файлов вместо API

---

## 🐳 Развертывание на сервере (Docker)

### Требования
- Linux сервер (Ubuntu/Debian рекомендуется)
- Docker и Docker Compose

### Шаг 1: Установка Docker

```bash
# Обновите систему
sudo apt update && sudo apt upgrade -y

# Установите Docker
sudo apt install -y docker.io

# Установите Docker Compose
sudo curl -L "https://github.com/docker/compose/releases/download/v2.27.0/docker-compose-$(uname -s)-$(uname -m)" -o /usr/local/bin/docker-compose
sudo chmod +x /usr/local/bin/docker-compose

# Добавьте пользователя в группу docker
sudo usermod -aG docker $USER

# Перелогиньтесь для применения изменений
```

### Шаг 2: Клонируйте репозиторий

```bash
git clone https://github.com/Powarar/calendar-fu.git
cd calendar-fu
```

### Шаг 3: Настройте переменные окружения (опционально)

Если в проекте есть `.env.example`:

```bash
cp .env.example .env
nano .env
```

Основные параметры:
- `RUZ_FILES_PATH` — путь к папке с файлами расписаний
- `REDIS_HOST` и `REDIS_PORT` — настройки Redis
- `PORT` — порт для backend-сервера

### Шаг 4: Запустите сервис

```bash
docker-compose up -d --build
```

Эта команда:
- Соберет Docker-образ из Dockerfile
- Запустит контейнеры: backend, frontend (nginx), redis
- Режим `-d` — запуск в фоновом режиме

### Шаг 5: Проверьте статус

```bash
# Проверить статус контейнеров
docker-compose ps

# Посмотреть логи backend
docker-compose logs -f backend

# Посмотреть логи frontend (nginx)
docker-compose logs -f frontend
```

### Шаг 6: Доступ к приложению

Сервис доступен по адресу:
```
http://<IP_вашего_сервера>/
```

**Основные эндпоинты:**
- Фронтенд: `http://<IP_сервера>/`
- API: `http://<IP_сервера>/api/...`
- Календари: `http://<IP_сервера>/calendar/...`

---

## 🔄 Обновление на сервере

После изменений в коде:

```bash
cd calendar-fu
git pull
docker-compose down
docker-compose up -d --build
```

---

## 🛠 Технологии

**Backend (Python 3.8+):**
- `asyncio` — асинхронное программирование
- `aiohttp` — асинхронный HTTP-сервер/клиент
- `aiomisc` — управление асинхронными сервисами
- `aioredis` — асинхронный клиент для Redis
- `icalendar` — генерация .ical файлов (RFC 5545)
- `marshmallow` — валидация и сериализация данных
- `ujson` — быстрая работа с JSON

**Инфраструктура:**
- Redis — кеширование данных
- Docker & Docker Compose — контейнеризация
- Nginx — веб-сервер и reverse proxy
- GitHub Actions — CI/CD автоматизация

---

## 📁 Структура проекта

```
calendar-fu/
├── calendar_fu/          # Исходный код backend
├── frontend/             # Статические файлы фронтенда
├── ruz_files/            # .ics файлы расписаний
├── docker-compose.yml    # Конфигурация Docker Compose
├── Dockerfile            # Сборка backend-образа
├── nginx.conf            # Конфигурация Nginx
├── requirements.txt      # Python зависимости
└── README.md
```

---

## 🐛 Отладка

**Остановить все контейнеры:**
```bash
docker-compose down
```

**Просмотр логов в реальном времени:**
```bash
docker-compose logs -f
```

**Перезапуск отдельного сервиса:**
```bash
docker-compose restart backend
```

**Зайти в контейнер для отладки:**
```bash
docker exec -it calendar-backend sh
```

---